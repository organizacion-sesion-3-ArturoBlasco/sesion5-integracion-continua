# En principi l'alumne sols va a rebre la definció de la funció a l'arxiu corresponent:
def suma(x,y):
    # L'alumne haurà d'implementar correctament el codi per passar el test
    return x + y

#Funció comprobar si un número es parell o no
def esParell(x):
    if x%2 == 0:
        return "par"
    else:
        return "impar"
